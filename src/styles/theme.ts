import { DefaultTheme } from 'styled-components';

const theme: DefaultTheme = {
  borderRadius: '8px',
  transitionDuration: '200ms',
  colors: {
    primary: '#25a69c',
    secondary: '#22302d',
  },
};

export { theme };
